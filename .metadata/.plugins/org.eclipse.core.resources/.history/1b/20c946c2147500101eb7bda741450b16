<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<%@ page import="java.util.List"%>
<%@ page import="DAO.User"%>
<%@ page isELIgnored="false"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>User/Admin Profile</title>
</head>
<body>

	<%
List<User> userList = null;
List<User> adminList = null;

if (session.getAttribute("UserName") != null) {
    userList = (List<User>) session.getAttribute("userdetails"); // match attribute name in repo
    if (userList != null && !userList.isEmpty()) {
        User u = userList.get(0);
%>
	<h1>
		UserName:
		<%= u.getUserName() %></h1>
	<h2>
		Email:
		<%= u.getEmail() %></h2>
	<h2>
		Password:
		<%= u.getPassword() %></h2>
	<%
    }
%>

	<%
    }
} else if (session.getAttribute("AdminName") != null) {
    adminList = (List<User>) session.getAttribute("Admindetails"); // match attribute name
    if (adminList != null && !adminList.isEmpty()) {
        User a = adminList.get(0);
%>
	<h1>
		UserName:
		<%= a.getUserName() %></h1>
	<h2>
		Email:
		<%= a.getEmail() %></h2>
	<h2>
		Password:
		<%= a.getPassword() %></h2>

	<h1>Add Movies</h1>
	<form>
		<!-- movie form fields here -->
	</form>
	<%
    } }
%>



</body>
</html>
