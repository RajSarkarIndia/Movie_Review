<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" isELIgnored="false" %>
<%@ page import="java.util.List" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Movies</title>
<style>
  ul {
    display: flex;
    justify-content: space-between;
    align-items: center;
    list-style: none;
    padding: 0;
    margin: 0 0 20px 0;
    width: 100%;
  }
  li {
    margin: 0;
    flex: 1;
    text-align: center;
  }
  li:first-child {
    text-align: left;
  }
  li:last-child {
    text-align: right;
  }
  form { margin: 0; }

  .movie-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-start;
    gap: 16px;
    width: 50%;
    margin: 0 auto;
  }
  .movie-item {
    border: 1px solid black;
    box-sizing: border-box;
    width: 240px;
    text-align: center;
    background: #fafafa;
    border-radius: 8px;
    overflow: hidden;
  }
  .movie-item img {
    width: 100%;
    height: 320px;
    object-fit: cover;
    display: block;
  }
  .movie-item h4 {
    margin: 8px 0;
  }

  @media (max-width: 900px) {
    .movie-container {
      width: 90%;
    }
    .movie-item { width: 47%; }
  }
  @media (max-width: 600px) {
    .movie-container { width: 100%; justify-content: center; }
    .movie-item { width: 100%; }
  }
</style>
</head>
<body>

<ul>
  <li>
    <form action="${pageContext.request.contextPath}/AllMovie">
      <input type="submit" value="All Movie">
    </form>
  </li>
  <li>
    <form action="/MovieReview/SelectedFlim">
      <input type="search" name="search" placeholder="Enter Flim Name" />
      <input type="submit" value="Search" />
    </form>
  </li>
  <li>
    <% if(session.getAttribute("UserName") == null && session.getAttribute("AdminName")==null) { %>
      <a href="LoginCheck.jsp">User Login</a>
      <a href="AdminLoginCheck.jsp">Admin Login</a>
    <% } else if(session.getAttribute("UserName") != null){ %>
      <%= session.getAttribute("UserName") %>
      <form action="/MovieReview/profile"><input type="submit" value="profile"></form>
    <% } else if(session.getAttribute("AdminName") != null){ %>
      <%= session.getAttribute("AdminName") %>
      <form action="/MovieReview/profile"><input type="submit" value="profile"></form>
    <% } %>
  </li>
</ul>

<h3 style="text-align:center;">New Movies</h3>
<div class="movie-container">
<%
    List movies = (List) session.getAttribute("movies");
    if (movies != null && !movies.isEmpty()) {
        for (int i = 0; i < Math.min(6, movies.size()); i++) {
            request.setAttribute("movie", movies.get(i));
%>
    <div class="movie-item">
      <a href="/MovieReview/SelectedFlim?search=${movie.movieName}">
        <img src="${pageContext.request.contextPath}/image/${movie.movieImageURL}" alt="${movie.movieName}" />
        <h4>${movie.movieName}</h4>
        <h4>Rating: ${movie.movieRating}</h4>
        <h4>Released: ${movie.releaseDate}</h4>
      </a>
    </div>
<%
        }
    } else {
%>
    <p>No movies available.</p>
<%
    }
%>
</div>

<h3 style="text-align:center;">Top Movie</h3>
<div class="movie-container">
<%
    List topmovie = (List) session.getAttribute("Topmovies");
    if (topmovie != null && !topmovie.isEmpty()) {
        for (int i = 0; i < Math.min(6, topmovie.size()); i++) {
            request.setAttribute("Topmovie", topmovie.get(i));
%>
    <div class="movie-item">
      <a href="/MovieReview/SelectedFlim?search=${Topmovie.movieName}">
        <img src="${pageContext.request.contextPath}/image/${Topmovie.movieImageURL}" alt="${Topmovie.movieName}" />
        <h4>${Topmovie.movieName}</h4>
        <h4>Rating: ${Topmovie.movieRating}</h4>
      </a>
    </div>
<%
        }
    }
%>
</div>
</body>
</html>
